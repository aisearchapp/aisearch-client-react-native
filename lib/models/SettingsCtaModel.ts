import SearchRecommendationCtaModel from './SearchRecommendationCtaModel';

/**
 * SettingsCtaModel
 *
 * This class extends SearchRecommendationCtaModel to represent Call-to-Action (CTA) data specifically used in settings.
 * While it currently inherits all functionality from its parent class without any modifications,
 * separating it into its own class allows for future customization and differentiation of CTA behavior within settings.
 */
class SettingsCtaModel extends SearchRecommendationCtaModel {
    // No additional functionality is added at this time.
}

export default SettingsCtaModel;
